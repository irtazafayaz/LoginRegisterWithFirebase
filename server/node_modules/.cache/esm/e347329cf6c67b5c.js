let mongoose,readdirSync;_f6c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_f6c‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);
// import router from "./routes/auth";

const express = require("express");
const cors = require("cors");
const morgan = require("morgan");
require("dotenv").config();

const app = express();

// Connect Db
mongoose
  .connect(process.env.DATABASE, {})
  .then(() => console.log("===> DB connected"))
  .catch((err) => _f6c‍.g.console.log("!!!! DB Error => ", err));

// Write all your middlewares here
app.use(cors());
app.use(morgan("dev"));

// You have only one single route
//app.use("/api", router);

// - What if there are numerous routes files?
readdirSync("./routes").map((dir) => {
  app.use("/api", require(`./routes/${dir}`))
});

// Routes

app.listen(process.env.PORT, () => {
  console.log("===> Server Started");
});
